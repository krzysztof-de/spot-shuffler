import { IPlace } from "@/backend/models/place";
import React from "react";

interface Props {
  place: IPlace;
}

const PlacesDatePicker = ({ place }: Props) => {
  return <div>PlacesDatePicker</div>;
};

export default PlacesDatePicker;
